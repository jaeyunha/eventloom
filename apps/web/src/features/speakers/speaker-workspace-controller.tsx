// biome-ignore-all format: compact controller boundary; extracted domain modules remain formatted
// biome-ignore-all assist/source/organizeImports: compact controller keeps its generated import order
"use client";import { CheckCircle2, Eye, FileText, ListTodo, Mail, RefreshCw, Search, Send, Upload, UserPlus, Users } from "lucide-react";import Link from "next/link";import { type ChangeEvent, type FormEvent, useCallback, useEffect, useMemo, useRef, useState } from "react";import { StatusBadge, WorkspaceHeader, WorkspaceListDetail, WorkspaceMetaItem } from "@/components/workspace";import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "../../components/ui/accordion";import { Alert, AlertDescription, AlertTitle } from "../../components/ui/alert";import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "../../components/ui/alert-dialog";import { Badge } from "../../components/ui/badge";import { Button } from "../../components/ui/button";import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "../../components/ui/card";import { Checkbox } from "../../components/ui/checkbox";import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "../../components/ui/collapsible";import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "../../components/ui/dialog";import { Empty, EmptyDescription, EmptyHeader, EmptyMedia, EmptyTitle } from "../../components/ui/empty";import { Field, FieldLabel, FieldLegend, FieldSet } from "../../components/ui/field";import { Input } from "../../components/ui/input";import { Progress } from "../../components/ui/progress";import { Select, SelectContent, SelectGroup, SelectItem, SelectTrigger, SelectValue } from "../../components/ui/select";import { Table, TableBody, TableCaption, TableCell, TableHead, TableHeader, TableRow } from "../../components/ui/table";import { Tabs, TabsContent, TabsList, TabsTrigger } from "../../components/ui/tabs";import { Textarea } from "../../components/ui/textarea";import { TemporalPicker } from "../../components/ui/temporal-picker";import adminStyles from "../admin/admin-shell.module.css";import { assertAdvancedSpeakerRevision, assertSpeakerHeadshotReplacement, assertSpeakerRosterScope, createSpeakerApi, ORGANIZER_HEADSHOT_ACCEPTED_TYPES, type SpeakerApi, SpeakerApiError, type SpeakerAsset, type SpeakerCreateInput, type SpeakerEmailPreview, type SpeakerEmailSend, type SpeakerEmailTemplate, type SpeakerImportPreview, type SpeakerInvitationPreview, type SpeakerInvitationResult, type SpeakerMutationStatus, type SpeakerProgressEnvelope, type SpeakerRecord, type SpeakerReminderEligibilityEnvelope, type SpeakerRosterEnvelope, type SpeakerSession, type SpeakerUpdateInput } from "./api";import { SpeakerAssetDownload, SpeakerAssetMetadata, SpeakerHeadshot, SpeakerStatusBadge } from "./speaker-assets";import { duplicateEmailConflicts, mergeProgressSummaries, mergeSpeaker, normalizeRoster, speakerProgressFor, speakerSecondaryLoadKey } from "./speaker-data-logic";import { FormMessage, MutationStatusMessage, SpeakerInvitationControls } from "./speaker-invitations";import { ProfileFields } from "./speaker-profile-fields";import { acceptedSpeakerSessions, organizerHeadshotPreviewPath, organizerHeadshotPreviewRequestKey, organizerHeadshotSubmissionId, validateOrganizerHeadshotFile, type OrganizerHeadshotUploadStatus } from "./speaker-headshot-logic"; import { dateLabel, dateTimeLabel, editDraftFor, emptyCreateDraft, errorMessage, filterSpeakerRoster, filterSpeakersByAttention, speakerProgressMatches, statusLabel, taskComplete, withTimeout, type SpeakerAttentionFilter } from "./speaker-roster-logic";import { SpeakerTaskStatusBadge } from "./speaker-task-logic";import { deadlineAfterEventWarning, deadlineTemporalPolicy } from "./speaker-temporal-policy";import { createSpeakerTaskAssignment, retainInvitationHistory, socialLinksFor, speakerInvitationReady, speakerOnboardingTaskDefinitions, travelLogisticsFor, validateSpeakerTaskAssignment, type SpeakerOnboardingTaskDraft } from "./speaker-task-model";import { ASYNC_ACTION_TIMEOUT_MS, DEFAULT_STATUS_OPTIONS, MAX_ORGANIZER_ONBOARDING_TASKS, SPEAKER_CUSTOM_FIELDS_CONTRACT_GAP, SPEAKER_ROSTER_COLUMNS, SPEAKER_WELCOME_EMAIL_STARTER, type CreateDraft, type EditDraft, type ProgressFilter, type SpeakerInvitationHistoryEntry, type SpeakerWorkspaceProps } from "./speaker-workspace-types";import styles from "./speaker-workspace.module.css";export function SpeakerWorkspaceController({organizationId,eventId,api: providedApi,}: SpeakerWorkspaceProps) {const [api, setApi] = useState<SpeakerApi | null>(providedApi ?? null);const [activeView, setActiveView] = useState<"roster" | "tasks" | "email">("roster");const [roster, setRoster] = useState<SpeakerRosterEnvelope | null>(null);const [progress, setProgress] = useState<SpeakerProgressEnvelope | null>(null);const [loading, setLoading] = useState(true);const [error, setError] = useState<string | null>(null);const [progressError, setProgressError] = useState<string | null>(null);const [notice, setNotice] = useState<string | null>(null);const [selectedId, setSelectedId] = useState<string | null>(null);const [query, setQuery] = useState("");const [statusFilter, setStatusFilter] = useState("all");const [selectedSpeakerIds, setSelectedSpeakerIds] = useState<readonly string[]>([]);const [emailTemplates, setEmailTemplates] = useState<readonly SpeakerEmailTemplate[]>([]);const [emailTemplateId, setEmailTemplateId] = useState("");const [emailTemplateVersion, setEmailTemplateVersion] = useState<number | undefined>(undefined);const [emailTemplateName, setEmailTemplateName] = useState("New speaker message");const [emailSubject, setEmailSubject] = useState("");const [emailHtml, setEmailHtml] = useState("");const [emailText, setEmailText] = useState("");const [emailPreview, setEmailPreview] = useState<SpeakerEmailPreview | null>(null);const [emailEditorMode, setEmailEditorMode] = useState<"visual" | "html" | "text">("visual");const [emailConfirmOpen, setEmailConfirmOpen] = useState(false);const [emailSends, setEmailSends] = useState<readonly SpeakerEmailSend[]>([]);const [emailSaveBusy, setEmailSaveBusy] = useState(false);const [emailPreviewBusy, setEmailPreviewBusy] = useState(false);const [emailSendBusy, setEmailSendBusy] = useState(false);const [emailHistoryBusy, setEmailHistoryBusy] = useState(false);const [emailSendIdempotencyKey, setEmailSendIdempotencyKey] = useState<string | null>(null);const [emailCreateTemplateId, setEmailCreateTemplateId] = useState<string | null>(null);const [emailNotice, setEmailNotice] = useState<string | null>(null);const [reminderEligibility, setReminderEligibility] =useState<SpeakerReminderEligibilityEnvelope | null>(null);const [visibleSecondaryContext, setVisibleSecondaryContext] = useState<string | null>(null);const [visibleProgressContext, setVisibleProgressContext] = useState<string | null>(null);const [sessionFilter, setSessionFilter] = useState("all");const [progressFilter, setProgressFilter] = useState<ProgressFilter>("all");const [attentionFilter, setAttentionFilter] = useState<SpeakerAttentionFilter>("all");const [filtersOpen, setFiltersOpen] = useState(false);const [showAdd, setShowAdd] = useState(false);const [showCsv, setShowCsv] = useState(false);const [createDraft, setCreateDraft] = useState<CreateDraft>(emptyCreateDraft);const [createIdempotencyKey, setCreateIdempotencyKey] = useState<string | null>(null);const [editDraft, setEditDraft] = useState<EditDraft | null>(null);const [editError, setEditError] = useState<string | null>(null);const [importPreview, setImportPreview] = useState<SpeakerImportPreview | null>(null);const [importIdempotencyKey, setImportIdempotencyKey] = useState<string | null>(null);const [importFileName, setImportFileName] = useState<string | null>(null);const [taskTitle, setTaskTitle] = useState("");const [taskDueAt, setTaskDueAt] = useState("");const [taskAssignees, setTaskAssignees] = useState<readonly string[]>([]);const [invitationPreview, setInvitationPreview] = useState<readonly SpeakerInvitationPreview[] | null>(null);const [invitationResult, setInvitationResult] = useState<SpeakerInvitationResult | null>(null);const [invitationResultParticipantId, setInvitationResultParticipantId] = useState<string | null>(null,);const [invitationError, setInvitationError] = useState<string | null>(null);const [invitationSendIdempotencyKey, setInvitationSendIdempotencyKey] = useState<string | null>(null,);const [invitationHistory, setInvitationHistory] = useState<readonly SpeakerInvitationHistoryEntry[]>([]);const [detailBusy, setDetailBusy] = useState(false);const [detailNotice, setDetailNotice] = useState<string | null>(null);const [headshotUploadStatus, setHeadshotUploadStatus] =useState<OrganizerHeadshotUploadStatus>("idle");const [headshotUploadMessage, setHeadshotUploadMessage] = useState<string | null>(null);const [headshotSubmissionId, setHeadshotSubmissionId] = useState<string | null>(null);const [headshotPreviewUrl, setHeadshotPreviewUrl] = useState<string | null>(null);const [headshotPreviewError, setHeadshotPreviewError] = useState<string | null>(null);const [headshotPreviewLoading, setHeadshotPreviewLoading] = useState(false);const [headshotPreviewRevision, setHeadshotPreviewRevision] = useState(0);const [headshotPreviewRetry, setHeadshotPreviewRetry] = useState(0);const [headshotAssetsByParticipant, setHeadshotAssetsByParticipant] = useState<Readonly<Record<string, SpeakerAsset>>>({});const [downloadUrls, setDownloadUrls] = useState<Readonly<Record<string, string>>>({});const [downloadErrors, setDownloadErrors] = useState<Readonly<Record<string, string>>>({});const [downloadBusyAssetId, setDownloadBusyAssetId] = useState<string | null>(null);const [invitationPreviewBusy, setInvitationPreviewBusy] = useState(false);const [invitationSendBusy, setInvitationSendBusy] = useState(false);const [importPreviewBusy, setImportPreviewBusy] = useState(false);const [importCommitBusy, setImportCommitBusy] = useState(false);const [taskBusy, setTaskBusy] = useState(false);const [saveBusy, setSaveBusy] = useState(false);const [profileMutationStatus, setProfileMutationStatus] = useState<SpeakerMutationStatus>("idle");const [profileMutationMessage, setProfileMutationMessage] = useState<string | null>(null);const [headshotMutationStatus, setHeadshotMutationStatus] =useState<SpeakerMutationStatus>("idle");const [headshotMutationMessage, setHeadshotMutationMessage] = useState<string | null>(null);const rosterRequestRef = useRef(0);const headshotRequestRef = useRef(0);const headshotPreviewKeyRef = useRef<string | null>(null); const headshotPreviewRequestDataRef = useRef<{ participantId: string | undefined; assetId: string | null | undefined; asset: SpeakerAsset | null }>({ participantId: undefined, assetId: undefined, asset: null }); const headshotPreviewSourceRef = useRef<{ api: SpeakerApi | null; version: number }>({ api: null, version: 0 });const importRequestRef = useRef(0);const emailSelectionSnapshotRef = useRef<string | null>(null);useEffect(() => {if (organizationId.length === 0 || eventId.length === 0) return;rosterRequestRef.current += 1;setRoster(null);setProgress(null);setLoading(true);setError(null);setProgressError(null);setSelectedId(null);setSelectedSpeakerIds([]);setEditDraft(null);setEditError(null);setDetailNotice(null);setHeadshotAssetsByParticipant({});setDownloadUrls({});setDownloadErrors({});setProfileMutationStatus("idle");setProfileMutationMessage(null);setHeadshotMutationStatus("idle");setHeadshotMutationMessage(null);setHeadshotUploadStatus("idle");setHeadshotUploadMessage(null);secondaryLoadRef.current = null;progressLoadRef.current = null;}, [eventId, organizationId]);const emailPreviewRequestRef = useRef(0);const secondaryLoadRef = useRef<{ api: SpeakerApi; key: string } | null>(null);const progressLoadRef = useRef<{ api: SpeakerApi; key: string; requestId: number } | null>(null);const emailSectionRef = useRef<HTMLDivElement | null>(null);const reminderSectionRef = useRef<HTMLDivElement | null>(null);const importBusy = importPreviewBusy || importCommitBusy;useEffect(() => {if (providedApi !== undefined) {setApi(providedApi);return;}try {setApi(createSpeakerApi("", organizationId, eventId));} catch (reason: unknown) {setApi(null);setError(errorMessage(reason));}}, [eventId, organizationId, providedApi]);useEffect(() => {if (api === null) {setLoading(false);return;}const requestId = rosterRequestRef.current + 1;rosterRequestRef.current = requestId;const controller = new AbortController();let active = true;let rosterTimedOut = false;const rosterTimeout = setTimeout(() => {rosterTimedOut = true;controller.abort();}, ASYNC_ACTION_TIMEOUT_MS);setLoading(true);setError(null);setProgressError(null);setProgress(null);setInvitationPreview(null);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);setInvitationSendIdempotencyKey(null);void api.list(controller.signal).then((nextRoster) => {const normalizedRoster = normalizeRoster(nextRoster, organizationId, eventId);if (!active || requestId !== rosterRequestRef.current) return;setRoster(normalizedRoster);setSelectedSpeakerIds((current) =>current.filter((participantId) =>normalizedRoster.speakers.some((speaker) => speaker.participantId === participantId),),);setSelectedId((current) =>current !== null &&normalizedRoster.speakers.some((speaker) => speaker.participantId === current)? current: (normalizedRoster.speakers[0]?.participantId ?? null),);setLoading(false);}).catch((reason: unknown) => {if (!active || requestId !== rosterRequestRef.current) return;setRoster(null);setProgress(null);setSelectedId(null);setSelectedSpeakerIds([]);setError(rosterTimedOut ? "Speaker roster refresh timed out. Try again." : errorMessage(reason),);}).finally(() => {clearTimeout(rosterTimeout);if (active && requestId === rosterRequestRef.current) setLoading(false);});return () => {active = false;controller.abort();};}, [api, eventId, organizationId]);const secondaryContextKey = `${organizationId}:${eventId}`;const progressSectionVisible =activeView === "tasks" || visibleProgressContext === secondaryContextKey;useEffect(() => {if (api === null ||roster === null ||loading ||!progressSectionVisible ||roster.organizationId !== organizationId ||roster.eventId !== eventId) {return;}const requestId = rosterRequestRef.current;const currentRequest = progressLoadRef.current;if (currentRequest?.api === api &&currentRequest.key === secondaryContextKey &&currentRequest.requestId === requestId) {return;}const request = { api, key: secondaryContextKey, requestId };progressLoadRef.current = request;const controller = new AbortController();let active = true;let settled = false;let timedOut = false;const timeoutId = setTimeout(() => {timedOut = true;controller.abort();}, ASYNC_ACTION_TIMEOUT_MS);setProgressError(null);void speakerProgressFor(api, roster.speakers, organizationId, eventId, controller.signal).then((nextProgress) => {if (!active || requestId !== rosterRequestRef.current) return;setProgress(nextProgress);setRoster((current) =>current === null ? current : mergeProgressSummaries(current, nextProgress),);}).catch((reason: unknown) => {if (!active || requestId !== rosterRequestRef.current) return;setProgress(null);setProgressError(timedOut ? "Speaker progress refresh timed out. Try again." : errorMessage(reason),);}).finally(() => {settled = true;clearTimeout(timeoutId);});return () => {active = false;controller.abort();clearTimeout(timeoutId);if (!settled && progressLoadRef.current === request) progressLoadRef.current = null;};}, [api, eventId, loading, organizationId, progressSectionVisible, roster, secondaryContextKey]);const secondarySectionsVisible =activeView === "tasks" ||activeView === "email" ||visibleSecondaryContext === secondaryContextKey;useEffect(() => {if (secondarySectionsVisible) return;const sections = [emailSectionRef.current, reminderSectionRef.current].filter((section): section is HTMLDivElement => section !== null,);if (sections.length === 0) return;if (typeof IntersectionObserver === "undefined") {setVisibleSecondaryContext(secondaryContextKey);return;}const observer = new IntersectionObserver((entries) => {if (!entries.some((entry) => entry.isIntersecting)) return;setVisibleSecondaryContext(secondaryContextKey);observer.disconnect();},{ rootMargin: "240px" },);for (const section of sections) observer.observe(section);return () => observer.disconnect();}, [secondaryContextKey, secondarySectionsVisible]);const currentSecondaryLoadKey = speakerSecondaryLoadKey(roster,organizationId,eventId,loading,secondarySectionsVisible,);useEffect(() => {const loadKey = currentSecondaryLoadKey;if (api === null || loadKey === null) return;if (secondaryLoadRef.current?.api === api && secondaryLoadRef.current.key === loadKey) return;secondaryLoadRef.current = { api, key: loadKey };let active = true;void withTimeout((signal) => api.listEmailTemplates(signal), "Email template load").then((templates) => {if (!active) return;setEmailTemplates(templates);const latest = templates.slice().sort((left, right) => right.updatedAt.localeCompare(left.updatedAt))[0];if (latest !== undefined) {setEmailTemplateId(latest.id);setEmailTemplateVersion(latest.version);setEmailTemplateName(latest.name);setEmailSubject(latest.subject);setEmailHtml(latest.html);setEmailText(latest.text);}}).catch(() => undefined);void withTimeout((signal) => api.listEmailHistory(signal), "Email history load").then((history) => {if (active) setEmailSends(history);}).catch(() => undefined);void withTimeout((signal) => api.getReminderEligibility({}, signal),"Reminder eligibility load",).then((eligibility) => {if (active) setReminderEligibility(eligibility);}).catch(() => undefined);return () => {active = false;};}, [api, currentSecondaryLoadKey]);const scopedRoster =roster !== null && roster.organizationId === organizationId && roster.eventId === eventId? roster: null;const eventTemporalContext = scopedRoster?.temporalContext;const taskDeadlinePolicy = eventTemporalContext === undefined ? null : deadlineTemporalPolicy(eventTemporalContext);const taskDeadlineWarning = eventTemporalContext === undefined ? null : deadlineAfterEventWarning(taskDueAt, eventTemporalContext);const scopedProgress =progress !== null && progress.organizationId === organizationId && progress.eventId === eventId? progress: null;const speakers = scopedRoster?.speakers ?? [];const rosterEmpty = !loading && scopedRoster !== null && speakers.length === 0;const selectedSpeaker = speakers.find((speaker) => speaker.participantId === selectedId) ?? null;const eligibleHeadshotSessions = useMemo(() => (selectedSpeaker === null ? [] : acceptedSpeakerSessions(selectedSpeaker.sessions)),[selectedSpeaker],);const selectedHeadshotSubmissionId = organizerHeadshotSubmissionId(selectedSpeaker?.sessions ?? [],headshotSubmissionId,);const cachedHeadshotAsset =selectedSpeaker === null? null: (headshotAssetsByParticipant[selectedSpeaker.participantId] ?? null);const selectedHeadshotAsset =selectedSpeaker === null || selectedSpeaker.headshotAssetId === null? null: (selectedSpeaker.assets.find((asset) => asset.assetId === selectedSpeaker.headshotAssetId,) ??(cachedHeadshotAsset?.assetId === selectedSpeaker.headshotAssetId? cachedHeadshotAsset: null));const duplicateEmailWarnings = useMemo(() => duplicateEmailConflicts(speakers), [speakers]);const emailAnyBusy = emailSaveBusy || emailPreviewBusy || emailSendBusy || emailHistoryBusy;const selectedEmailTemplate = emailTemplates.find((template) => template.id === emailTemplateId && template.version === emailTemplateVersion,);const statusOptions = useMemo(() => {const values = new Set<string>([...DEFAULT_STATUS_OPTIONS,...speakers.map((speaker) => speaker.status),]);return [...values];}, [speakers]);const sessionOptions = useMemo(() => {const values = new Map<string, string>();for (const speaker of speakers) {for (const session of speaker.sessions) {values.set(session.submissionId, session.title);}}return [...values.entries()].sort((left, right) => left[1].localeCompare(right[1]));}, [speakers]);const normalizedQuery = query.trim().toLocaleLowerCase();const filteredSpeakers = useMemo(() =>filterSpeakerRoster(filterSpeakersByAttention(speakers, attentionFilter),scopedProgress?.rows ?? [],{query,status: statusFilter,session: sessionFilter,progress: progressFilter,},),[attentionFilter,progressFilter,query,scopedProgress?.rows,sessionFilter,speakers,statusFilter,],);const attentionCounts = useMemo(() => ({all: speakers.length,overdue: filterSpeakersByAttention(speakers, "overdue").length,"awaiting-invite": filterSpeakersByAttention(speakers, "awaiting-invite").length,"duplicate-email": filterSpeakersByAttention(speakers, "duplicate-email").length,inactive: filterSpeakersByAttention(speakers, "inactive").length,}),[speakers],);const progressRows = useMemo(() =>(scopedProgress?.rows ?? []).filter((row) =>speakerProgressMatches(row.tasks, progressFilter),),[scopedProgress?.rows, progressFilter],);const onboardingTaskDefinitions = useMemo(() => speakerOnboardingTaskDefinitions(scopedProgress?.rows ?? []),[scopedProgress?.rows],);const eligibleReminderItems = useMemo(() => reminderEligibility?.items.filter((item) => item.eligible) ?? [],[reminderEligibility],);const ineligibleReminderItems = useMemo(() => reminderEligibility?.items.filter((item) => !item.eligible) ?? [],[reminderEligibility],);const selectedInvitationPreview =selectedSpeaker === null? []: (invitationPreview ?? []).filter((preview) => preview.participantId === selectedSpeaker.participantId,);const invitationPreviewCount = selectedInvitationPreview.length;const invitationReady =selectedSpeaker !== null && speakerInvitationReady(invitationPreview ?? [], selectedSpeaker);const selectedInvitationResultRecipient =selectedSpeaker === null ||invitationResult === null ||invitationResultParticipantId !== selectedSpeaker.participantId? null: (invitationResult.recipients.find((recipient) => recipient.participantId === selectedSpeaker.participantId,) ?? null);const hasActiveRosterFilters =normalizedQuery.length > 0 ||statusFilter !== "all" ||sessionFilter !== "all" ||progressFilter !== "all";const selectedVisibleSpeakerIds = filteredSpeakers.map((speaker) => speaker.participantId).filter((participantId) => selectedSpeakerIds.includes(participantId));const allVisibleSelected =filteredSpeakers.length > 0 && selectedVisibleSpeakerIds.length === filteredSpeakers.length;const emailPreviewCurrent =emailPreview !== null &&emailPreview.organizationId === organizationId &&emailPreview.eventId === eventId &&emailPreview.templateId === emailTemplateId &&emailPreview.templateVersion === emailTemplateVersion &&emailPreview.recipientIds.length === selectedSpeakerIds.length &&selectedSpeakerIds.every((participantId) => emailPreview.recipientIds.includes(participantId));const invalidateEmailPreview = useCallback(() => {emailPreviewRequestRef.current += 1;setEmailPreview(null);setEmailSendIdempotencyKey(null);setEmailConfirmOpen(false);setEmailPreviewBusy(false);}, []);useEffect(() => {const snapshot = [...selectedSpeakerIds].sort().join("\u0000");const previous = emailSelectionSnapshotRef.current;if (previous !== null && previous !== snapshot) invalidateEmailPreview();emailSelectionSnapshotRef.current = snapshot;}, [invalidateEmailPreview, selectedSpeakerIds]);if (headshotPreviewSourceRef.current.api !== api) { headshotPreviewSourceRef.current = { api, version: headshotPreviewSourceRef.current.version + 1 }; } const headshotPreviewRequestKey = organizerHeadshotPreviewRequestKey(headshotPreviewSourceRef.current.version, headshotPreviewRetry, selectedSpeaker?.participantId, selectedSpeaker?.headshotAssetId, selectedHeadshotAsset); headshotPreviewRequestDataRef.current = { participantId: selectedSpeaker?.participantId, assetId: selectedSpeaker?.headshotAssetId, asset: selectedHeadshotAsset }; useEffect(() => { const { participantId, assetId, asset: selectedHeadshotAsset } = headshotPreviewRequestDataRef.current; const requestKey = headshotPreviewRequestKey; const requestId = headshotRequestRef.current + 1; headshotRequestRef.current = requestId;let active = true;if (api === null || participantId === undefined || assetId === undefined || assetId === null) {headshotPreviewKeyRef.current = null;setHeadshotPreviewUrl(null);setHeadshotPreviewError(null);setHeadshotPreviewLoading(false);return () => {active = false;};}if (requestKey !== null && requestKey === headshotPreviewKeyRef.current) {return () => {active = false;};}headshotPreviewKeyRef.current = requestKey;setHeadshotPreviewUrl(null);setHeadshotPreviewError(null);setHeadshotPreviewLoading(true);void (async () => {try {if (selectedHeadshotAsset === null) {const assets = await withTimeout((signal) => api.getAssets(participantId, signal),"Headshot metadata load",);if (!active || requestId !== headshotRequestRef.current) return;const linked = assets.find((asset) => asset.assetId === assetId);if (linked === undefined) {setHeadshotPreviewError("The linked headshot file is unavailable.");return;}setHeadshotAssetsByParticipant((current) => ({...current,[participantId]: linked,}));return;}if (selectedHeadshotAsset.status !== "ready") {throw new Error("The linked headshot is not ready yet.");}const contentType = selectedHeadshotAsset.contentType.trim().toLowerCase();if (!ORGANIZER_HEADSHOT_ACCEPTED_TYPES.includes(contentType as (typeof ORGANIZER_HEADSHOT_ACCEPTED_TYPES)[number],)) {throw new Error("The linked asset is not a supported headshot image.");}const grant = await withTimeout((signal) => api.getDownloadGrant(selectedHeadshotAsset.assetId, signal),"Headshot preview",);const previewPath = organizerHeadshotPreviewPath(grant.url);if (previewPath === null) {throw new Error("The private headshot preview did not return a same-origin API path.");}if (!active || requestId !== headshotRequestRef.current) return;setHeadshotPreviewUrl(previewPath);setHeadshotPreviewRevision((current) => current + 1);} catch (reason: unknown) {if (active && requestId === headshotRequestRef.current) {setHeadshotPreviewError(errorMessage(reason));}} finally {if (active && requestId === headshotRequestRef.current) {setHeadshotPreviewLoading(false);}}})();return () => {active = false;}; }, [api, headshotPreviewRequestKey]);function clearRosterFilters(): void {setQuery("");setStatusFilter("all");setSessionFilter("all");setProgressFilter("all");setAttentionFilter("all");}function openSelectedEmail(): void {setActiveView("email");}function toggleSpeakerSelection(participantId: string): void {setSelectedSpeakerIds((current) =>current.includes(participantId)? current.filter((candidate) => candidate !== participantId): [...current, participantId],);invalidateEmailPreview();setEmailNotice(null);}function toggleVisibleSpeakerSelection(): void {const visibleIds = filteredSpeakers.map((speaker) => speaker.participantId);setSelectedSpeakerIds((current) =>allVisibleSelected? current.filter((participantId) => !visibleIds.includes(participantId)): [...new Set([...current, ...visibleIds])],);invalidateEmailPreview();setEmailNotice(null);}function clearSpeakerSelection(): void {setSelectedSpeakerIds([]);invalidateEmailPreview();setEmailNotice(null);}function updateCreate(field: keyof CreateDraft, value: string | boolean): void {setCreateDraft((current) => ({ ...current, [field]: value }) as CreateDraft);setCreateIdempotencyKey(null);setNotice(null);}function updateEdit(field: keyof CreateDraft, value: string | boolean): void {setEditDraft((current) =>current === null ? current : ({ ...current, [field]: value } as EditDraft),);setEditError(null);setProfileMutationStatus("idle");setProfileMutationMessage(null);setNotice(null);setInvitationPreview(null);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);setInvitationSendIdempotencyKey(null);}function applyAuthoritativeRoster(nextRoster: SpeakerRosterEnvelope, message?: string): void {try {const normalizedRoster = normalizeRoster(nextRoster, organizationId, eventId);const requestId = rosterRequestRef.current + 1;rosterRequestRef.current = requestId;setRoster(normalizedRoster);setLoading(false);setSelectedSpeakerIds((current) =>current.filter((participantId) =>normalizedRoster.speakers.some((speaker) => speaker.participantId === participantId),),);setSelectedId((current) =>current !== null &&normalizedRoster.speakers.some((speaker) => speaker.participantId === current)? current: (normalizedRoster.speakers[0]?.participantId ?? null),);setError(null);setProgressError(null);setProgress(null);setInvitationPreview(null);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);setInvitationSendIdempotencyKey(null);if (message) setNotice(message);if (api !== null) {const progressController = new AbortController();let progressTimedOut = false;const progressTimeout = setTimeout(() => {progressTimedOut = true;progressController.abort();}, ASYNC_ACTION_TIMEOUT_MS);void speakerProgressFor(api,normalizedRoster.speakers,organizationId,eventId,progressController.signal,).then((nextProgress) => {if (requestId !== rosterRequestRef.current) return;setProgress(nextProgress);setRoster((current) =>current === null ? current : mergeProgressSummaries(current, nextProgress),);}).catch((reason: unknown) => {if (requestId !== rosterRequestRef.current) return;setProgressError(progressTimedOut? "Speaker progress refresh timed out. Try again.": errorMessage(reason),);}).finally(() => {clearTimeout(progressTimeout);});}} catch (reason: unknown) {setRoster(null);setProgress(null);setSelectedId(null);setSelectedSpeakerIds([]);setError(errorMessage(reason));}}async function reload(message?: string): Promise<SpeakerRosterEnvelope | null> {if (api === null) {setError("The speaker API is unavailable.");return null;}const requestId = rosterRequestRef.current + 1;rosterRequestRef.current = requestId;const controller = new AbortController();let rosterTimedOut = false;const rosterTimeout = setTimeout(() => {rosterTimedOut = true;controller.abort();}, ASYNC_ACTION_TIMEOUT_MS);setLoading(true);setError(null);setProgressError(null);setProgress(null);setInvitationPreview(null);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);setInvitationSendIdempotencyKey(null);try {const nextRoster = normalizeRoster(await api.list(controller.signal),organizationId,eventId,);if (requestId !== rosterRequestRef.current) return null;setRoster(nextRoster);setSelectedSpeakerIds((current) =>current.filter((participantId) =>nextRoster.speakers.some((speaker) => speaker.participantId === participantId),),);setSelectedId((current) =>current !== null && nextRoster.speakers.some((speaker) => speaker.participantId === current)? current: (nextRoster.speakers[0]?.participantId ?? null),);setLoading(false);if (message) setNotice(message);const progressController = new AbortController();let progressTimedOut = false;const progressTimeout = setTimeout(() => {progressTimedOut = true;progressController.abort();}, ASYNC_ACTION_TIMEOUT_MS);void speakerProgressFor(api,nextRoster.speakers,organizationId,eventId,progressController.signal,).then((nextProgress) => {if (requestId !== rosterRequestRef.current) return;setProgress(nextProgress);setRoster((current) =>current === null ? current : mergeProgressSummaries(current, nextProgress),);setProgressError(null);}).catch((reason: unknown) => {if (requestId !== rosterRequestRef.current) return;setProgressError(progressTimedOut? "Speaker progress refresh timed out. Try again.": errorMessage(reason),);}).finally(() => {clearTimeout(progressTimeout);});return nextRoster;} catch (reason: unknown) {if (requestId === rosterRequestRef.current) {if (reason instanceof Error &&/different organization|different event|invalid|duplicate participant/iu.test(reason.message,)) {setRoster(null);setProgress(null);setSelectedId(null);setSelectedSpeakerIds([]);}setError(rosterTimedOut ? "Speaker roster refresh timed out. Try again." : errorMessage(reason),);}return null;} finally {clearTimeout(rosterTimeout);if (requestId === rosterRequestRef.current) setLoading(false);}}async function createSpeaker(event: FormEvent<HTMLFormElement>): Promise<void> {event.preventDefault();if (api === null) {setNotice("Speaker creation is unavailable until the organizer speaker API is configured.");return;}const idempotencyKey = createIdempotencyKey ?? crypto.randomUUID();setCreateIdempotencyKey(idempotencyKey);const input: SpeakerCreateInput = {idempotencyKey,displayName: createDraft.displayName.trim(),email: createDraft.email.trim(),jobTitle: createDraft.title.trim(),company: createDraft.company.trim(),biography: createDraft.biography.trim(),socialLinks: socialLinksFor(createDraft),travelLogistics: travelLogisticsFor(createDraft),status: createDraft.status,};if (!input.displayName || !input.email) {setNotice("Name and email are required.");return;}setSaveBusy(true);setNotice(null);try {const created = await api.create(input);setCreateDraft(emptyCreateDraft());setCreateIdempotencyKey(null);setShowAdd(false);applyAuthoritativeRoster(created, "Speaker added to the roster.");} catch (reason: unknown) {setNotice(errorMessage(reason));} finally {setSaveBusy(false);}}function beginEdit(speaker: SpeakerRecord): void {setSelectedId(speaker.participantId);setHeadshotSubmissionId(null);setHeadshotUploadStatus("idle");setHeadshotUploadMessage(null);setHeadshotMutationStatus("idle");setHeadshotMutationMessage(null);setEditDraft(editDraftFor(speaker, eventTemporalContext?.timeZone));setProfileMutationStatus("idle");setProfileMutationMessage(null);setEditError(null);setInvitationPreview(null);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);setInvitationSendIdempotencyKey(null);setDetailNotice(null);}async function saveSpeaker(event: FormEvent<HTMLFormElement>): Promise<void> {event.preventDefault();if (api === null || editDraft === null || selectedSpeaker === null) return;const participantId = selectedSpeaker.participantId;const expectedVersion = editDraft.expectedVersion;const input: SpeakerUpdateInput = {expectedVersion,displayName: editDraft.displayName.trim(),email: editDraft.email.trim(),jobTitle: editDraft.title.trim(),company: editDraft.company.trim(),biography: editDraft.biography.trim(),socialLinks: socialLinksFor(editDraft),travelLogistics: travelLogisticsFor(editDraft),status: editDraft.status,};if (!input.displayName || !input.email) {setEditError("Name and email are required.");setProfileMutationStatus("failure");setProfileMutationMessage("Name and email are required.");return;}setInvitationPreview(null);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);setInvitationSendIdempotencyKey(null);setSaveBusy(true);setProfileMutationStatus("saving");setProfileMutationMessage("Saving speaker profile…");setEditError(null);try {const updatedRoster = assertSpeakerRosterScope(await api.update(participantId, input),organizationId,eventId,);const updated = updatedRoster.speakers.find((speaker) => speaker.participantId === participantId,);if (updated === undefined) {throw new TypeError("The saved speaker is missing from the roster.");}assertAdvancedSpeakerRevision(updated, participantId, expectedVersion, eventId);setProfileMutationStatus("pending");setProfileMutationMessage("Profile write accepted. Reloading authoritative speaker data…");const reloaded = await reload();const persisted = reloaded?.speakers.find((speaker) => speaker.participantId === participantId,);if (persisted === undefined) {throw new TypeError("The reloaded speaker is missing from the roster.");}assertAdvancedSpeakerRevision(persisted, participantId, expectedVersion, eventId);setEditDraft(editDraftFor(persisted, eventTemporalContext?.timeZone));setProfileMutationStatus("saved");setProfileMutationMessage(`Saved at revision ${persisted.version}.`);setNotice("Speaker profile saved and reloaded from the server.");} catch (reason: unknown) {const conflict =reason instanceof SpeakerApiError &&(reason.status === 409 || reason.code === "CONFLICT" || reason.code === "VERSION_CONFLICT");if (conflict) {setProfileMutationStatus("conflict");setProfileMutationMessage("Conflict detected. Authoritative speaker data was reloaded.");const reloaded = await reload();const current = reloaded?.speakers.find((speaker) => speaker.participantId === participantId,);if (current !== undefined) setEditDraft(editDraftFor(current, eventTemporalContext?.timeZone));setEditError("This speaker changed elsewhere. Review the reloaded values before saving.");} else {setProfileMutationStatus("failure");setProfileMutationMessage(errorMessage(reason));setEditError(errorMessage(reason));}} finally {setSaveBusy(false);}}async function previewCsv(event: ChangeEvent<HTMLInputElement>): Promise<void> {const input = event.currentTarget;const file = input.files?.[0];if (!file) {input.value = "";return;}const requestId = importRequestRef.current + 1;importRequestRef.current = requestId;input.value = "";setImportFileName(file.name);setImportPreview(null);setImportIdempotencyKey(null);setImportPreviewBusy(true);setNotice(null);try {if (api === null) {throw new Error("CSV import is unavailable until the organizer speaker API is configured.");}const preview = await withTimeout((signal) => api.previewImport(file, signal),"CSV validation",);if (requestId !== importRequestRef.current) return;setImportPreview(preview);setImportIdempotencyKey(crypto.randomUUID());setNotice(`CSV preview ready: ${preview.validRows.length} valid row${preview.validRows.length === 1 ? "" : "s"}.`,);} catch (reason: unknown) {if (requestId === importRequestRef.current) setNotice(errorMessage(reason));} finally {if (requestId === importRequestRef.current) {setImportPreviewBusy(false);input.value = "";}}}async function commitCsv(): Promise<void> {if (api === null || importPreview === null || importPreview.validRows.length === 0) return;const idempotencyKey = importIdempotencyKey ?? crypto.randomUUID();setImportIdempotencyKey(idempotencyKey);setImportCommitBusy(true);setNotice(null);try {const rowCount = importPreview.validRows.length;const imported = await withTimeout((signal) =>api.commitImport({rows: importPreview.validRows,idempotencyKey,},signal,),"CSV import",);setImportPreview(null);setImportFileName(null);setImportIdempotencyKey(null);applyAuthoritativeRoster(imported,`CSV import committed: ${rowCount} valid row${rowCount === 1 ? "" : "s"}.`,);} catch (reason: unknown) {setNotice(errorMessage(reason));} finally {setImportCommitBusy(false);}}function toggleAssignee(participantId: string): void {setTaskAssignees((current) =>current.includes(participantId)? current.filter((candidate) => candidate !== participantId): [...current, participantId],);}async function assignTask(event: FormEvent<HTMLFormElement>): Promise<void> {event.preventDefault();if (api === null) {setNotice("Task assignment is unavailable until the organizer speaker API is configured.");return;}if (progress === null || progressError !== null) {setNotice("Wait for API-backed onboarding progress to load before assigning another task.");return;}const draft = {title: taskTitle,dueAt: taskDueAt,participantIds: taskAssignees,} satisfies SpeakerOnboardingTaskDraft;const validationError = validateSpeakerTaskAssignment(draft, onboardingTaskDefinitions.length);if (validationError !== null) {setNotice(validationError);return;}const input = createSpeakerTaskAssignment(draft);setTaskBusy(true);setNotice(null);try {const latest = await withTimeout(async (signal) => {const latestRoster = normalizeRoster(await api.list(signal), organizationId, eventId);const latestProgress = await speakerProgressFor(api,latestRoster.speakers,organizationId,eventId,signal,);return { latestRoster, latestProgress };}, "Onboarding task preflight");setProgress(latest.latestProgress);setRoster(mergeProgressSummaries(latest.latestRoster, latest.latestProgress));setInvitationPreview(null);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);setInvitationSendIdempotencyKey(null);const latestValidationError = validateSpeakerTaskAssignment(draft,speakerOnboardingTaskDefinitions(latest.latestProgress.rows).length,);if (latestValidationError !== null) {setNotice(latestValidationError);return;}const taskEnvelope = await api.assignTasks(input);if (taskEnvelope.organizationId !== organizationId || taskEnvelope.eventId !== eventId) {throw new TypeError("The speaker task response belongs to a different organization or event.",);}const returnedAssignees = new Set(taskEnvelope.tasks.map((task) => task.participantId));if (taskEnvelope.tasks.length !== input.participantIds.length ||returnedAssignees.size !== input.participantIds.length ||input.participantIds.some((participantId) => !returnedAssignees.has(participantId)) ||taskEnvelope.tasks.some((task) =>task.title !== input.title ||task.description !== input.description ||task.dueAt !== input.dueAt ||task.type !== "general",)) {throw new TypeError("The speaker task response does not match the selected assignees.");}setRoster((current) =>current === null? current: {...current,speakers: current.speakers.map((speaker) => {const added = taskEnvelope.tasks.filter((task) => task.participantId === speaker.participantId,).length;return added === 0? speaker: {...speaker,taskSummary: {...speaker.taskSummary,total: speaker.taskSummary.total + added,},};}),},);setProgress((current) => {const rows =current?.rows ??speakers.map((speaker) => ({participantId: speaker.participantId,displayName: speaker.displayName,tasks: [],}));return {organizationId,eventId,rows: rows.map((row) => {const assigned = taskEnvelope.tasks.filter((task) => task.participantId === row.participantId,);return assigned.length === 0 ? row : { ...row, tasks: [...row.tasks, ...assigned] };}),};});setTaskTitle("");setTaskDueAt("");setTaskAssignees([]);void api.getReminderEligibility().then(setReminderEligibility).catch(() => undefined);setNotice("General action task assigned to selected speakers.");} catch (reason: unknown) {setNotice(errorMessage(reason));} finally {setTaskBusy(false);}}async function refreshEmailHistory(): Promise<void> {if (api === null) {setEmailNotice("Email history is unavailable until the organizer speaker API is configured.");return;}setEmailHistoryBusy(true);setEmailNotice(null);try {const history = await withTimeout((signal) => api.listEmailHistory(signal),"Email history refresh",);setEmailSends(history);setEmailNotice(`Email history refreshed: ${history.length} send${history.length === 1 ? "" : "s"}.`,);} catch (reason: unknown) {setEmailNotice(errorMessage(reason));} finally {setEmailHistoryBusy(false);}}async function saveEmailTemplate(): Promise<SpeakerEmailTemplate | null> {if (api === null) {setEmailNotice("Email templates are unavailable until the organizer speaker API is configured.",);return null;}invalidateEmailPreview();setEmailSaveBusy(true);setEmailNotice(null);try {const newTemplateId = emailCreateTemplateId ?? `speaker-email-draft:${crypto.randomUUID()}`;if (emailTemplateId.length === 0) setEmailCreateTemplateId(newTemplateId);const template = emailTemplateId? await withTimeout((signal) =>api.createEmailTemplateVersion({templateId: emailTemplateId,subject: emailSubject,html: emailHtml,text: emailText,},signal,),"Email template save",): await withTimeout((signal) =>api.createEmailTemplate({templateId: newTemplateId,name: emailTemplateName,subject: emailSubject,html: emailHtml,text: emailText,},signal,),"Email template save",);setEmailTemplates((current) => [...current.filter((candidate) => !(candidate.id === template.id && candidate.version === template.version),),template,]);setEmailTemplateId(template.id);setEmailCreateTemplateId(null);setEmailTemplateVersion(template.version);setEmailTemplateName(template.name);setEmailSubject(template.subject);setEmailHtml(template.html);setEmailText(template.text);setEmailNotice(`Template version ${template.version} saved.`);setEmailPreview(null);setEmailSendIdempotencyKey(null);return template;} catch (reason: unknown) {setEmailNotice(errorMessage(reason));return null;} finally {setEmailSaveBusy(false);}}async function previewBulkEmail(): Promise<void> {if (api === null) {setEmailNotice("Bulk email is unavailable until the organizer speaker API is configured.");return;}if (selectedSpeakerIds.length === 0) {setEmailNotice("Select at least one speaker before previewing an email.");return;}invalidateEmailPreview();const requestId = emailPreviewRequestRef.current;setEmailPreviewBusy(true);setEmailNotice(null);try {const recipientIds = [...selectedSpeakerIds];let templateId = emailTemplateId;let templateVersion = emailTemplateVersion;const newTemplateId = emailCreateTemplateId ?? `speaker-email-draft:${crypto.randomUUID()}`;if (templateId.length === 0) setEmailCreateTemplateId(newTemplateId);if (templateId.length === 0) {const created = await withTimeout((signal) =>api.createEmailTemplate({templateId: newTemplateId,name: emailTemplateName,subject: emailSubject,html: emailHtml,text: emailText,},signal,),"Email template preparation",);if (requestId !== emailPreviewRequestRef.current) return;templateId = created.id;templateVersion = created.version;setEmailTemplateId(created.id);setEmailTemplateVersion(created.version);setEmailTemplateName(created.name);setEmailSubject(created.subject);setEmailHtml(created.html);setEmailText(created.text);setEmailTemplates((current) => [...current.filter((candidate) => !(candidate.id === created.id && candidate.version === created.version),),created,]);}const preview = await withTimeout((signal) =>api.previewEmails({participantIds: recipientIds,templateId,...(templateVersion === undefined ? {} : { templateVersion }),},signal,),"Email merge preview",);if (requestId !== emailPreviewRequestRef.current) return;if (preview.organizationId !== organizationId || preview.eventId !== eventId) {throw new Error("The email preview belongs to a different event. Create a new preview.");}setEmailPreview(preview);setEmailTemplateId(preview.templateId);setEmailCreateTemplateId(null);setEmailTemplateVersion(preview.templateVersion);setEmailSendIdempotencyKey(null);setEmailNotice(`Merge preview ready for ${preview.recipientIds.length} selected speaker${preview.recipientIds.length === 1 ? "" : "s"}.`,);} catch (reason: unknown) {if (requestId === emailPreviewRequestRef.current) setEmailNotice(errorMessage(reason));} finally {if (requestId === emailPreviewRequestRef.current) setEmailPreviewBusy(false);}}async function sendBulkEmail(): Promise<void> {if (api === null || !emailPreviewCurrent || emailPreview === null) {setEmailNotice("Create a current merge preview before queueing the email.");setEmailConfirmOpen(false);return;}const preview = emailPreview;const idempotencyKey = emailSendIdempotencyKey ?? crypto.randomUUID();setEmailSendIdempotencyKey(idempotencyKey);setEmailSendBusy(true);setEmailNotice(null);try {const send = await withTimeout((signal) =>api.sendEmails({previewId: preview.id,idempotencyKey,},signal,),"Speaker email queue",);setEmailSends((current) => [send,...current.filter((candidate) => candidate.id !== send.id),]);setEmailNotice(`Speaker email ${send.status} for ${send.recipientIds.length} recipient${send.recipientIds.length === 1 ? "" : "s"}. Queue history is retained below.`,);} catch (reason: unknown) {setEmailNotice(errorMessage(reason));} finally {setEmailSendBusy(false);}}async function previewSelectedSpeakerInvitation(): Promise<void> {if (api === null || selectedSpeaker === null) {setInvitationError("Portal invitations are unavailable until the organizer speaker API is configured.",);return;}const participantId = selectedSpeaker.participantId;setInvitationPreviewBusy(true);setInvitationPreview(null);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);setInvitationSendIdempotencyKey(null);try {const preview = await withTimeout(() => api.previewInvitations({ participantIds: [participantId] }),"Portal invitation preview",);if (preview.length !== 1 || preview[0]?.participantId !== participantId) {throw new TypeError("The invitation preview does not match the selected speaker.");}setInvitationPreview(preview);} catch (reason: unknown) {setInvitationError(errorMessage(reason));} finally {setInvitationPreviewBusy(false);}}async function sendSelectedSpeakerInvitation(): Promise<void> {if (api === null || selectedSpeaker === null || !invitationReady) {setInvitationError("Preview an eligible portal invitation before sending it.");return;}const participantId = selectedSpeaker.participantId;const preview = selectedInvitationPreview;const idempotencyKey = invitationSendIdempotencyKey ?? crypto.randomUUID();setInvitationSendIdempotencyKey(idempotencyKey);setInvitationSendBusy(true);setInvitationResult(null);setInvitationResultParticipantId(null);setInvitationError(null);try {const result = await withTimeout(() =>api.sendInvitations({participantIds: [participantId],templateId: "speaker-welcome",idempotencyKey,}),"Portal invitation send",);const selectedRecipient = result.recipients.find((recipient) => recipient.participantId === participantId,);if (selectedRecipient === undefined) {throw new TypeError("The invitation result does not include the selected speaker.");}setInvitationResult(result);setInvitationResultParticipantId(participantId);setInvitationHistory((current) => retainInvitationHistory(current, preview, result));setInvitationSendIdempotencyKey(null);} catch (reason: unknown) {setInvitationError(errorMessage(reason));} finally {setInvitationSendBusy(false);}}async function refreshDetails(): Promise<void> {if (api === null || selectedSpeaker === null) {setDetailNotice("Session and deliverable details are unavailable until the organizer speaker API is configured.",);return;}setDetailBusy(true);setDetailNotice(null);try {const [sessions, assets] = await Promise.all([api.getSessions(selectedSpeaker.participantId),api.getAssets(selectedSpeaker.participantId),]);setRoster((current) =>current === null? current: mergeSpeaker(current, selectedSpeaker.participantId, {sessions,assets,updatedAt: new Date().toISOString(),}),);setDetailNotice("Session assignments and deliverables refreshed.");} catch (reason: unknown) {setDetailNotice(errorMessage(reason));} finally {setDetailBusy(false);}}async function requestAssetDownload(asset: SpeakerAsset): Promise<void> {if (api === null || asset.status !== "ready" || downloadBusyAssetId !== null) return;const assetId = asset.assetId;setDownloadBusyAssetId(assetId);setDownloadErrors((current) => {const { [assetId]: _previousError, ...remaining } = current;return remaining;});try {const grant = await withTimeout((signal) => api.getDownloadGrant(assetId, signal),"Asset download",);if (grant.url.trim().length === 0) {throw new Error("The private download capability returned an empty URL.");}setDownloadUrls((current) => ({ ...current, [assetId]: grant.url }));} catch (reason: unknown) {setDownloadErrors((current) => ({ ...current, [assetId]: errorMessage(reason) }));} finally {setDownloadBusyAssetId((current) => (current === assetId ? null : current));}}function retryHeadshotPreview(): void {setHeadshotPreviewRetry((current) => current + 1);}function markHeadshotPreviewFailed(): void {setHeadshotPreviewUrl(null);setHeadshotPreviewLoading(false);setHeadshotPreviewError("The secure headshot preview could not be rendered.");}async function uploadOrganizerHeadshot(event: ChangeEvent<HTMLInputElement>): Promise<void> {const input = event.currentTarget;const file = input.files?.[0];input.value = "";if (file === undefined) return;const validationError = validateOrganizerHeadshotFile(file);if (validationError !== null) {setHeadshotUploadStatus("error");setHeadshotMutationStatus("failure");setHeadshotUploadMessage(validationError);setHeadshotMutationMessage(validationError);return;}if (api === null || selectedSpeaker === null || api.replaceHeadshot === undefined) {const message ="Organizer headshot upload is unavailable until the private upload API is provisioned.";setHeadshotUploadStatus("error");setHeadshotMutationStatus("failure");setHeadshotUploadMessage(message);setHeadshotMutationMessage(message);return;}if (selectedHeadshotSubmissionId === null) {setHeadshotUploadStatus("error");setHeadshotUploadMessage(eligibleHeadshotSessions.length > 1? "Choose an accepted session before replacing this headshot.": "Headshot replacement requires an accepted session owned by this speaker.",);return;}const participantId = selectedSpeaker.participantId;const expectedVersion = selectedSpeaker.version;const supersedesAssetId = selectedSpeaker.headshotAssetId ?? undefined;setHeadshotUploadStatus("busy");setHeadshotMutationStatus("saving");setHeadshotMutationMessage(`Uploading ${file.name}…`);setHeadshotUploadMessage(`Uploading ${file.name}…`);try {const replacement = assertSpeakerHeadshotReplacement(await api.replaceHeadshot({participantId,submissionId: selectedHeadshotSubmissionId,file,expectedVersion,...(supersedesAssetId === undefined ? {} : { supersedesAssetId }),}),eventId,participantId,expectedVersion,);setHeadshotMutationStatus("pending");setHeadshotMutationMessage("Headshot write accepted. Reloading authoritative speaker data…");setHeadshotUploadMessage("Upload accepted. Reloading speaker data…");const reloaded = await reload();const persisted = reloaded?.speakers.find((speaker) => speaker.participantId === participantId,);if (persisted === undefined) {throw new TypeError("The reloaded speaker is missing from the roster.");}assertAdvancedSpeakerRevision(persisted, participantId, expectedVersion, eventId);if (persisted.headshotAssetId !== replacement.asset.id) {throw new TypeError("The reloaded speaker does not point to the uploaded headshot.");}setEditDraft((current) => (current === null ? current : editDraftFor(persisted)));setHeadshotPreviewUrl(null);setHeadshotPreviewError(null);setHeadshotPreviewRevision((current) => current + 1);setHeadshotUploadStatus("success");setHeadshotMutationStatus("saved");setHeadshotMutationMessage(`Saved at revision ${persisted.version}.`);setHeadshotUploadMessage(`Headshot uploaded for ${persisted.displayName}.`);} catch (reason: unknown) {const conflict =reason instanceof SpeakerApiError &&(reason.status === 409 || reason.code === "CONFLICT" || reason.code === "VERSION_CONFLICT");if (conflict) {setHeadshotMutationStatus("conflict");setHeadshotMutationMessage("Conflict detected. Authoritative speaker data was reloaded.");setHeadshotUploadStatus("error");setHeadshotUploadMessage("Headshot upload conflicted; review the reloaded speaker data.");await reload();} else {const message = errorMessage(reason);setHeadshotMutationStatus("failure");setHeadshotMutationMessage(message);setHeadshotUploadStatus("error");setHeadshotUploadMessage(message);}}}return (<div className={styles.workspace}><WorkspaceHeader eyebrow="Event operations / Speakers"title="Speaker operations"description="Manage people and profiles in Roster, assign onboarding action items for speakers to complete in their portal, and use Email for speaker-only outreach; broader announcements belong in Communications."status={<StatusBadge tone="neutral">{speakers.length} speakers</StatusBadge>}metadata={<><WorkspaceMetaItem>Organization {organizationId}</WorkspaceMetaItem><WorkspaceMetaItem>Event {eventId}</WorkspaceMetaItem></>}actions={<><Button variant="outline"type="button"onClick={() => void reload()}disabled={loading}><RefreshCw data-icon="inline-start" />{loading ? "Refreshing…" : "Refresh roster"}</Button>{!rosterEmpty ? (<Button variant="default" type="button" onClick={() => setShowAdd(true)}><UserPlus data-icon="inline-start" />Add speaker</Button>) : null}</>}/>{error ? <FormMessage message={error} error /> : null}{notice ? <FormMessage message={notice} /> : null}<Dialog open={showAdd} onOpenChange={setShowAdd}><DialogContent className={styles.dialogContent}><DialogHeader><DialogTitle>Add speaker</DialogTitle><DialogDescription>Capture identity and profile details before sending an optional portal invitation.</DialogDescription></DialogHeader><form className={styles.actionsStack} onSubmit={(event) => void createSpeaker(event)}><ProfileFields draft={createDraft} onChange={updateCreate} disabled={saveBusy} {...(eventTemporalContext === undefined ? {} : { temporalContext: eventTemporalContext })} /><Field><FieldLabel htmlFor="create-speaker-status">Workflow status</FieldLabel><Select value={createDraft.status}onValueChange={(value) => updateCreate("status", value)}disabled={saveBusy}><SelectTrigger id="create-speaker-status" className={styles.control}><SelectValue placeholder="Select workflow status" /></SelectTrigger><SelectContent><SelectGroup>{statusOptions.map((status) => (<SelectItem key={status} value={status}>{statusLabel(status)}</SelectItem>))}</SelectGroup></SelectContent></Select></Field><Button variant="default" type="submit" disabled={saveBusy || api === null}><CheckCircle2 data-icon="inline-start" />{saveBusy ? "Saving…" : "Save speaker"}</Button><p className={styles.muted} role="note">Headshot upload is completed by the speaker in their portal.{" "}{SPEAKER_CUSTOM_FIELDS_CONTRACT_GAP}</p></form><DialogFooter><Button variant="outline" type="button" onClick={() => setShowAdd(false)}>Cancel</Button></DialogFooter></DialogContent></Dialog><Tabs value={activeView}onValueChange={(value) => setActiveView(value as "roster" | "tasks" | "email")}className={styles.tabs}><TabsList variant="line" aria-label="Speaker workspace views"><TabsTrigger id="roster-tab" aria-controls="roster-view" value="roster"><Users data-icon="inline-start" />Roster</TabsTrigger><TabsTrigger id="tasks-tab" aria-controls="tasks-view" value="tasks"><ListTodo data-icon="inline-start" />Onboarding</TabsTrigger><TabsTrigger id="email-tab" aria-controls="email-view" value="email"><Mail data-icon="inline-start" />Email</TabsTrigger></TabsList><TabsContent value="roster"id="roster-view"aria-labelledby="roster-tab"className={styles.view}>{rosterEmpty ? (<Card className={styles.firstRunCard}><CardHeader><CardTitle id="roster-empty-heading">Start your speaker roster</CardTitle><CardDescription>Add speakers one at a time or import a CSV to give this event a clear home for profiles, sessions, and delivery details.</CardDescription></CardHeader><CardContent className={styles.actionsStack}><Empty className={styles.empty}><EmptyHeader><EmptyMedia variant="icon"><Users /></EmptyMedia><EmptyTitle>No speakers added yet</EmptyTitle><EmptyDescription>Start with a speaker profile or bring in your existing roster. You can invite speakers to their portal and assign onboarding after people are here.</EmptyDescription></EmptyHeader><div className={styles.actions}><Button variant="default" type="button" onClick={() => setShowAdd(true)}><UserPlus data-icon="inline-start" />Add speaker</Button><Button variant="outline" type="button" onClick={() => setShowCsv((current) => !current)} aria-expanded={showCsv} aria-controls="speaker-csv-import"><FileText data-icon="inline-start" />{showCsv ? "Hide CSV import" : "Import CSV"}</Button></div></Empty></CardContent></Card>) : (<><section className={styles.attentionStrip} aria-label="Speaker attention filters">{([["all", "All speakers"],["overdue", "Overdue tasks"],["awaiting-invite", "Awaiting invite"],["duplicate-email", "Duplicate emails"],["inactive", "Inactive"],] as const).map(([value, label]) => (<button key={value}className={styles.attentionFilter}type="button"aria-pressed={attentionFilter === value}onClick={() => setAttentionFilter(value)}><span>{label}</span><strong>{attentionCounts[value]}</strong></button>))}</section><Card className={styles.panel} aria-busy={loading}><CardHeader className={styles.panelHeader}><div><CardTitle id="roster-heading">Roster</CardTitle><CardDescription>Manage people and profile/delivery records for this event, then open a speaker for details.</CardDescription></div><Badge variant="outline">{scopedRoster ? `${filteredSpeakers.length} of ${speakers.length}` : "Loading"}</Badge></CardHeader><CardContent className={styles.actionsStack}><div className={styles.rosterToolbar}><Field className={styles.searchField}><FieldLabel className={adminStyles.srOnly} htmlFor="speaker-search">Search speakers</FieldLabel><div className={styles.inputWithIcon}><Search aria-hidden="true" /><Input id="speaker-search"aria-label="Search speakers"placeholder="Search speakers"value={query}onChange={(event) => setQuery(event.target.value)}/></div></Field><Button variant="outline"type="button"aria-expanded={filtersOpen}onClick={() => setFiltersOpen((current) => !current)}>Filters</Button>{hasActiveRosterFilters || attentionFilter !== "all" ? (<Button variant="ghost" type="button" onClick={clearRosterFilters}>Clear</Button>) : null}</div>{filtersOpen ? (<div className={styles.filterPanel}><Field><FieldLabel className={adminStyles.srOnly} htmlFor="speaker-status-filter">Filter by status</FieldLabel><Select value={statusFilter} onValueChange={setStatusFilter}><SelectTrigger id="speaker-status-filter" aria-label="Filter by status"><SelectValue placeholder="All statuses" /></SelectTrigger><SelectContent><SelectGroup><SelectItem value="all">All statuses</SelectItem>{statusOptions.map((status) => (<SelectItem key={status} value={status}>{statusLabel(status)}</SelectItem>))}</SelectGroup></SelectContent></Select></Field><Field><FieldLabel className={adminStyles.srOnly} htmlFor="speaker-session-filter">Filter by session</FieldLabel><Select value={sessionFilter} onValueChange={setSessionFilter}><SelectTrigger id="speaker-session-filter" aria-label="Filter by session"><SelectValue placeholder="All sessions" /></SelectTrigger><SelectContent><SelectGroup><SelectItem value="all">All sessions</SelectItem>{sessionOptions.map(([sessionId, title]) => (<SelectItem key={sessionId} value={sessionId}>{title}</SelectItem>))}</SelectGroup></SelectContent></Select></Field><Field><FieldLabel className={adminStyles.srOnly} htmlFor="speaker-progress-filter">Filter by task progress</FieldLabel><Select value={progressFilter}onValueChange={(value) => setProgressFilter(value as ProgressFilter)}><SelectTrigger id="speaker-progress-filter"aria-label="Filter by task progress"><SelectValue placeholder="All task progress" /></SelectTrigger><SelectContent><SelectGroup><SelectItem value="all">All task progress</SelectItem><SelectItem value="complete">Complete</SelectItem><SelectItem value="incomplete">Incomplete</SelectItem></SelectGroup></SelectContent></Select></Field></div>) : null}{hasActiveRosterFilters ? (<p className={styles.muted} role="status" aria-live="polite">Showing {filteredSpeakers.length} of {speakers.length} speakers after filters.</p>) : null}{selectedSpeakerIds.length > 0 ? (<div className={styles.selectionBar} role="status"><span><strong>{selectedSpeakerIds.length}</strong> selected for email</span><div className={styles.actions}><Button size="sm" type="button" onClick={openSelectedEmail}><Mail data-icon="inline-start" />Compose email</Button><Button variant="outline"size="sm"type="button"onClick={toggleVisibleSpeakerSelection}disabled={filteredSpeakers.length === 0}>{allVisibleSelected ? "Deselect visible" : "Select visible"}</Button><Button variant="ghost" size="sm" type="button" onClick={clearSpeakerSelection}>Clear</Button></div></div>) : null}{loading ? (<FormMessage message={scopedRoster ? "Refreshing speaker roster…" : "Loading speaker roster…"}/>) : null}{duplicateEmailWarnings.length > 0 ? (<FormMessage error message={`Duplicate speaker email conflict: ${duplicateEmailWarnings
                    .map(
                      (conflict) =>
                        `${conflict.email} (${conflict.speakers.map((speaker) => speaker.displayName).join(", ")})`,
                    )
                    .join("; ")}. Each authoritative speaker remains visible.`}/>) : null}<WorkspaceListDetail className={styles.rosterGrid}listLabel="Speaker roster"detailLabel="Selected speaker record"list={<div className={styles.rosterList}>{!loading && scopedRoster && speakers.length === 0 ? (<Empty className={styles.empty}><EmptyHeader><EmptyMedia variant="icon"><Users /></EmptyMedia><EmptyTitle>No speakers yet</EmptyTitle><EmptyDescription>Add a speaker or use the Import CSV control below to start this event roster.</EmptyDescription></EmptyHeader></Empty>) : null}{!loading &&scopedRoster &&speakers.length > 0 &&filteredSpeakers.length === 0 ? (<Empty className={styles.empty}><EmptyHeader><EmptyTitle>No matching speakers</EmptyTitle><EmptyDescription>No speakers match the current search and filters. Clear them to restore the roster.</EmptyDescription></EmptyHeader></Empty>) : null}{filteredSpeakers.length > 0 ? (<div className={styles.speakerTableViewport}><Table className={styles.speakerTable}><TableCaption className={styles.srOnly}>Event speaker roster</TableCaption><TableHeader><TableRow><TableHead><span className={styles.srOnly}>Select</span></TableHead>{SPEAKER_ROSTER_COLUMNS.map((column) => (<TableHead key={column}>{column}</TableHead>))}</TableRow></TableHeader><TableBody>{filteredSpeakers.map((speaker) => (<TableRow className={styles.speakerRow}key={speaker.participantId}aria-current={selectedId === speaker.participantId ? "true" : undefined}data-state={selectedId === speaker.participantId ? "selected" : undefined}><TableCell className={styles.checkboxCell}><Checkbox id={`roster-selection-${speaker.participantId}`}aria-label={`Select ${speaker.displayName}`}checked={selectedSpeakerIds.includes(speaker.participantId)}onCheckedChange={() =>toggleSpeakerSelection(speaker.participantId)}/></TableCell><TableHead scope="row" className={styles.speakerIdentityCell}><div className={styles.speakerCopy}><Button variant="ghost"size="sm"className={styles.speakerName}type="button"onClick={() => beginEdit(speaker)}>{speaker.displayName}</Button><span className={styles.speakerMeta}>{speaker.email}</span><span className={styles.speakerMeta}>{speaker.jobTitle || speaker.company? `${speaker.jobTitle ?? ""}${speaker.jobTitle && speaker.company ? " · " : ""}${speaker.company ?? ""}`: "Profile details pending"}</span></div></TableHead><TableCell><SpeakerStatusBadge status={speaker.status} /></TableCell><TableCell className={styles.numericCell}>{speaker.sessions.length} session{speaker.sessions.length === 1 ? "" : "s"}</TableCell><TableCell className={styles.taskCell}>{speaker.taskSummary.completed} / {speaker.taskSummary.total}{" "}tasks{speaker.taskSummary.overdue > 0? ` · ${speaker.taskSummary.overdue} overdue`: ""}</TableCell><TableCell className={styles.actionCell}><Button variant="ghost"size="sm"type="button"onClick={() => beginEdit(speaker)}>Open</Button></TableCell></TableRow>))}</TableBody></Table></div>) : null}</div>}detail={selectedSpeaker ? (<Card className={styles.detail} aria-labelledby="speaker-detail-heading"><CardHeader className={styles.detailHeader}><div><p className={styles.eyebrow}>Speaker record</p><CardTitle id="speaker-detail-heading">{selectedSpeaker.displayName}</CardTitle><CardDescription>{selectedSpeaker.email} ·{" "}<SpeakerStatusBadge status={selectedSpeaker.status} /></CardDescription></div><div className={styles.actions}><Button variant="outline"size="sm"type="button"onClick={() => void refreshDetails()}disabled={detailBusy || api === null}><RefreshCw data-icon="inline-start" />{detailBusy ? "Refreshing details…" : "Refresh details"}</Button><SpeakerInvitationControls previewBusy={invitationPreviewBusy}sendBusy={invitationSendBusy}disabled={api === null}canSend={invitationReady}onPreview={() => void previewSelectedSpeakerInvitation()}onSend={() => void sendSelectedSpeakerInvitation()}/></div></CardHeader><CardContent className={styles.actionsStack}>{invitationPreviewCount > 0 ? (<Alert><Mail /><AlertTitle>Invitation preview ready</AlertTitle><AlertDescription>{invitationPreviewCount} speaker previewed.{" "}{invitationReady ? "Eligible to send." : "Sending is blocked."}{" "}Sending remains a separate explicit action.<ul className={styles.list} aria-label="Portal invitation preview">{selectedInvitationPreview.map((preview) => (<li key={preview.participantId}><strong>{statusLabel(preview.state)}</strong> ·{" "}{preview.recipientEmail || "No deliverable email"}</li>))}</ul></AlertDescription></Alert>) : null}{invitationResult &&selectedInvitationResultRecipient &&invitationResultParticipantId === selectedSpeaker.participantId ? (<FormMessage message={`Invitation ${selectedInvitationResultRecipient.status} for ${selectedInvitationResultRecipient.recipientEmail}.`}error={selectedInvitationResultRecipient.status === "failed"}/>) : null}{invitationError ? <FormMessage message={invitationError} error /> : null}{invitationHistory.length > 0 ? (<div className={styles.detailBlock}><h3 className={styles.subheading}>Portal invitation send history</h3><ul className={styles.list} aria-label="Portal invitation send history">{invitationHistory.map((entry) => (<li key={`${entry.result.idempotencyKey}:${entry.occurredAt}`}><strong>{statusLabel(entry.result.status)}</strong> ·{" "}{entry.preview.map((preview) => preview.recipientEmail || preview.participantId,).join(", ")}{" "}· {dateTimeLabel(entry.occurredAt)} UTC</li>))}</ul><p className={styles.muted}>Sent invitations persist in the durable server email history; use Refresh history in the Email view to reload the authoritative record.</p></div>) : null}{detailNotice ? (<FormMessage message={detailNotice}error={detailNotice.includes("unavailable") || detailNotice.includes("could")}/>) : null}<Card className={styles.uploadPanel}><CardHeader><CardTitle className={styles.subheading}>Headshot</CardTitle><CardDescription>Secure event-scoped preview and organizer replacement.</CardDescription></CardHeader><CardContent className={styles.actionsStack}><SpeakerHeadshot speakerName={selectedSpeaker.displayName}asset={selectedHeadshotAsset}imageUrl={headshotPreviewUrl}loading={headshotPreviewLoading}error={headshotPreviewError}revision={headshotPreviewRevision}onRetry={retryHeadshotPreview}onImageError={markHeadshotPreviewFailed}/>{eligibleHeadshotSessions.length > 1 ? (<Field><FieldLabel htmlFor="speaker-headshot-session">Session for headshot replacement</FieldLabel><Select value={headshotSubmissionId ?? ""}onValueChange={(value) => {setHeadshotSubmissionId(value);setHeadshotUploadStatus("idle");setHeadshotUploadMessage(null);}}><SelectTrigger id="speaker-headshot-session"><SelectValue placeholder="Choose an accepted session" /></SelectTrigger><SelectContent>{eligibleHeadshotSessions.map((session) => (<SelectItem key={session.submissionId}value={session.submissionId}>{session.title}</SelectItem>))}</SelectContent></Select></Field>) : eligibleHeadshotSessions.length === 0 ? (<p className={styles.muted} role="status">Headshot replacement requires an accepted session owned by this speaker.</p>) : null}<Field><FieldLabel htmlFor="speaker-headshot-upload">Upload or replace headshot</FieldLabel><Input id="speaker-headshot-upload"type="file"accept={ORGANIZER_HEADSHOT_ACCEPTED_TYPES.join(",")}onChange={(event) => void uploadOrganizerHeadshot(event)}disabled={headshotUploadStatus === "busy" ||api === null ||api.replaceHeadshot === undefined ||selectedHeadshotSubmissionId === null}/></Field><p className={styles.muted}>Accepted headshot types: JPEG, PNG, or WebP; maximum size 5 MB.Uploads use the event-scoped organizer private upload flow.</p>{headshotUploadMessage ? (<FormMessage message={headshotUploadMessage}error={headshotUploadStatus === "error"}/>) : null}<MutationStatusMessage label="Headshot"status={headshotMutationStatus}message={headshotMutationMessage}/></CardContent></Card>{editDraft ? (<form className={styles.detailBlock}onSubmit={(event) => void saveSpeaker(event)}><ProfileFields draft={editDraft}onChange={updateEdit}disabled={saveBusy}{...(eventTemporalContext === undefined ? {} : { temporalContext: eventTemporalContext })}/><Field><FieldLabel htmlFor="edit-speaker-status">Workflow status</FieldLabel><Select value={editDraft.status}onValueChange={(value) => updateEdit("status", value)}disabled={saveBusy}><SelectTrigger id="edit-speaker-status"><SelectValue placeholder="Select workflow status" /></SelectTrigger><SelectContent><SelectGroup>{statusOptions.map((status) => (<SelectItem key={status} value={status}>{statusLabel(status)}</SelectItem>))}</SelectGroup></SelectContent></Select></Field><MutationStatusMessage label="Profile"status={profileMutationStatus}message={profileMutationMessage}/>{editError ? <FormMessage message={editError} error /> : null}<div className={styles.actions}><Button variant="default"type="submit"disabled={saveBusy || api === null}><CheckCircle2 data-icon="inline-start" />{profileMutationStatus === "pending"? "Pending…": saveBusy? "Saving…": "Save profile changes"}</Button><Badge variant="outline">Version {editDraft.expectedVersion}</Badge></div></form>) : (<Button variant="outline"type="button"onClick={() => beginEdit(selectedSpeaker)}>Edit profile</Button>)}<div className={styles.detailGrid}><Card size="sm"><CardHeader><CardTitle className={styles.subheading}>Session assignments</CardTitle><CardDescription>Authoritative agenda links.</CardDescription></CardHeader><CardContent className={styles.actionsStack}>{selectedSpeaker.sessions.length === 0 ? (<Empty><EmptyTitle>No sessions linked</EmptyTitle><EmptyDescription>No sessions are linked to this speaker yet.</EmptyDescription></Empty>) : (<ul className={styles.list}>{selectedSpeaker.sessions.map((session: SpeakerSession) => (<li key={session.submissionId} className={styles.preview}><strong>{session.title}</strong><Badge variant="outline">{statusLabel(session.status)}</Badge></li>))}</ul>)}<div className={styles.actions}><Button variant="outline"type="button"disabled title="Session linking is managed by the agenda service.">Assign a session</Button><Button variant="outline" asChild><Link href={`/admin/organizations/${encodeURIComponent(organizationId)}/events/${encodeURIComponent(eventId)}/agenda`}>Open Agenda</Link></Button></div><p className={styles.muted}>Session linking is managed in Agenda; this workspace shows the authoritative assignments.</p></CardContent></Card><Card size="sm"><CardHeader><CardTitle className={styles.subheading}>Uploaded deliverables</CardTitle><CardDescription>Private event files and headshots.</CardDescription></CardHeader><CardContent>{selectedSpeaker.assets.length === 0 ? (<Empty><EmptyTitle>No deliverables</EmptyTitle><EmptyDescription>No uploaded headshot or deliverables are available.</EmptyDescription></Empty>) : (<ul className={styles.list}>{selectedSpeaker.assets.map((asset: SpeakerAsset) => (<li key={asset.assetId} className={styles.preview}><strong>{asset.fileName}</strong><SpeakerAssetMetadata asset={asset} /><SpeakerAssetDownload asset={asset}downloadUrl={downloadUrls[asset.assetId] ?? null}busy={downloadBusyAssetId === asset.assetId}disabled={api === null || downloadBusyAssetId !== null}error={downloadErrors[asset.assetId] ?? null}onRequest={requestAssetDownload}/></li>))}</ul>)}</CardContent></Card></div></CardContent></Card>) : (<Empty className={styles.empty}><EmptyHeader><EmptyMedia variant="icon"><Users /></EmptyMedia><EmptyTitle>Select a speaker</EmptyTitle><EmptyDescription>Select a speaker to see profile and delivery details.</EmptyDescription></EmptyHeader></Empty>)}/></CardContent></Card></>)}<Collapsible open={showCsv} onOpenChange={setShowCsv} className={styles.importDetails}>{!rosterEmpty ? (<CollapsibleTrigger asChild><Button variant="outline" type="button"><FileText data-icon="inline-start" />{showCsv ? "Hide CSV import" : "Import CSV"}</Button></CollapsibleTrigger>) : null}<CollapsibleContent id="speaker-csv-import" className={styles.importBody}><Card aria-busy={importBusy}><CardHeader><CardTitle>Import speaker roster</CardTitle><CardDescription>Preview validation before committing rows. Invalid rows are never written to this event.</CardDescription></CardHeader><CardContent className={styles.actionsStack}><Field><FieldLabel htmlFor="speaker-csv">Speakers CSV</FieldLabel><Input id="speaker-csv"type="file"accept=".csv,text/csv"onChange={(event) => void previewCsv(event)}disabled={importCommitBusy || api === null}/></Field>{importFileName ? (<p className={styles.muted}>Selected file: <strong>{importFileName}</strong></p>) : null}{importBusy ? <FormMessage message="Validating CSV…" /> : null}{importPreview ? (<div className={styles.actionsStack}><div className={styles.actions}><Badge variant="secondary">{importPreview.validRows.length} valid</Badge><Badge variant="destructive">{importPreview.invalidRows.length} invalid</Badge></div>{importPreview.invalidRows.length > 0 ? (<ul className={styles.list} aria-label="CSV validation errors">{importPreview.invalidRows.map((issue) => (<li key={`${issue.rowNumber}-${issue.field ?? "row"}-${issue.message}`}>Row {issue.rowNumber}{issue.field ? ` · ${issue.field}` : ""}: {issue.message}</li>))}</ul>) : (<p className={styles.muted}>All previewed rows passed required identity validation.</p>)}<Button variant="default"type="button"onClick={() => void commitCsv()}disabled={importCommitBusy ||importPreviewBusy ||importPreview.validRows.length === 0 ||api === null}><Upload data-icon="inline-start" />{importCommitBusy? "Importing…": `Commit ${importPreview.validRows.length} valid row${importPreview.validRows.length === 1 ? "" : "s"}`}</Button></div>) : null}</CardContent></Card></CollapsibleContent></Collapsible></TabsContent><TabsContent value="tasks"id="tasks-view"aria-labelledby="tasks-tab"className={styles.view}><Card className={styles.panel}><CardHeader className={styles.panelHeader}><div><p className={styles.eyebrow}>Assign a new action</p><CardTitle id="tasks-heading">Speaker onboarding</CardTitle><CardDescription>Organizers assign action items that speakers complete in their portal. Email is for messages that do not require task completion.</CardDescription></div><Badge variant="outline">{onboardingTaskDefinitions.length} / {MAX_ORGANIZER_ONBOARDING_TASKS} task definitions</Badge></CardHeader><CardContent className={styles.actionsStack}>{rosterEmpty ? (<Empty className={styles.empty}><EmptyHeader><EmptyMedia variant="icon"><ListTodo /></EmptyMedia><EmptyTitle>Add speakers to start onboarding</EmptyTitle><EmptyDescription>Add or import speakers before assigning action items they can complete in their portal.</EmptyDescription></EmptyHeader><div className={styles.actions}><Button variant="default" type="button" onClick={() => {setActiveView("roster");setShowAdd(true);}}><UserPlus data-icon="inline-start" />Add speaker</Button><Button variant="outline" type="button" onClick={() => {setActiveView("roster");setShowCsv(true);}}><FileText data-icon="inline-start" />Import CSV</Button></div></Empty>) : (<>{!progressSectionVisible ? (<Button variant="outline"type="button"onClick={() => setVisibleProgressContext(secondaryContextKey)}disabled={api === null || loading || roster === null}><RefreshCw data-icon="inline-start" />Load task progress</Button>) : progress === null && progressError === null ? (<FormMessage message="Loading task progress…" />) : null}<form className={styles.actionsStack} onSubmit={(event) => void assignTask(event)}><div className={styles.fieldGrid}><Field><FieldLabel htmlFor="task-title">Task title</FieldLabel><Input id="task-title"value={taskTitle}onChange={(event) => setTaskTitle(event.target.value)}placeholder="Confirm participation"required disabled={taskBusy}/></Field><Field style={{ gridColumn: "1 / -1" }}><TemporalPicker id="task-due-date"mode="single"precision="date"value={taskDueAt}label="Due date"eyebrow="Task deadline"description="Choose a deadline on or after today in the event timezone."minimumDateTime={taskDeadlinePolicy?.minimumDate}onChange={setTaskDueAt}disabled={taskBusy}/>{taskDeadlineWarning ? <p className={styles.muted} role="status">{taskDeadlineWarning}</p> : null}</Field></div><FieldSet className={styles.detailBlock}><FieldLegend variant="label">Assign to speakers</FieldLegend>{speakers.length === 0 ? (<Empty><EmptyTitle>Add speakers first</EmptyTitle><EmptyDescription>Add speakers before assigning a task.</EmptyDescription></Empty>) : (<div className={styles.checkboxGrid}>{speakers.map((speaker) => (<Field key={speaker.participantId}orientation="horizontal"className={styles.checkboxField}><Checkbox id={`task-assignee-${speaker.participantId}`}aria-label={`Assign task to ${speaker.displayName}`}checked={taskAssignees.includes(speaker.participantId)}onCheckedChange={() => toggleAssignee(speaker.participantId)}disabled={taskBusy}/><FieldLabel htmlFor={`task-assignee-${speaker.participantId}`}>Assign task to {speaker.displayName}</FieldLabel></Field>))}</div>)}</FieldSet><div className={styles.actions}><Button variant="default"type="submit"disabled={taskBusy ||api === null ||speakers.length === 0 ||progress === null ||progressError !== null ||onboardingTaskDefinitions.length >= MAX_ORGANIZER_ONBOARDING_TASKS}><ListTodo data-icon="inline-start" />{taskBusy? "Assigning…": onboardingTaskDefinitions.length >= MAX_ORGANIZER_ONBOARDING_TASKS? "Three onboarding tasks configured": "Assign onboarding task"}</Button><Badge variant="outline">Task type: action / mark complete</Badge></div></form>{onboardingTaskDefinitions.length > 0 ? (<Table><TableCaption className={adminStyles.srOnly}>API-loaded organizer onboarding task definitions</TableCaption><TableHeader><TableRow><TableHead>Task</TableHead><TableHead>Due</TableHead><TableHead>Assignees</TableHead></TableRow></TableHeader><TableBody>{onboardingTaskDefinitions.map((definition) => (<TableRow key={definition.definitionId}><TableHead scope="row">{definition.title}</TableHead><TableCell>{dateLabel(definition.dueAt)}</TableCell><TableCell>{definition.participantIds.map((participantId) => {const assignee = speakers.find((speaker) => speaker.participantId === participantId,);return assignee === undefined? participantId: `${assignee.displayName} (${participantId})`;}).join(", ")}</TableCell></TableRow>))}</TableBody></Table>) : null}</>)}</CardContent></Card>{!rosterEmpty ? (<><Card className={styles.panel}><CardHeader className={styles.panelHeader}><div><CardTitle id="progress-heading">Onboarding progress</CardTitle><CardDescription>List-level general-task completion, including changes speakers make in their portal.</CardDescription></div><Field><FieldLabel className={adminStyles.srOnly} htmlFor="task-progress-filter">Filter task progress</FieldLabel><Select value={progressFilter}onValueChange={(value) => setProgressFilter(value as ProgressFilter)}><SelectTrigger id="task-progress-filter" aria-label="Filter task progress"><SelectValue placeholder="All progress" /></SelectTrigger><SelectContent><SelectGroup><SelectItem value="all">All progress</SelectItem><SelectItem value="complete">Complete</SelectItem><SelectItem value="incomplete">Incomplete</SelectItem></SelectGroup></SelectContent></Select></Field></CardHeader><CardContent>{progressError ? (<FormMessage message={`Progress unavailable: ${progressError}`} error />) : null}{!progressError && progress && progressRows.length === 0 ? (<Empty><EmptyTitle>No progress matches</EmptyTitle><EmptyDescription>No speakers match this progress filter.</EmptyDescription></Empty>) : null}{!progressError && progress && progressRows.length > 0 ? (<Table><TableCaption className={adminStyles.srOnly}>Speaker task completion progress</TableCaption><TableHeader><TableRow><TableHead>Speaker</TableHead><TableHead>Tasks and due dates</TableHead><TableHead>Progress</TableHead></TableRow></TableHeader><TableBody>{progressRows.map((row) => {const completed = row.tasks.filter((task) =>taskComplete(task.status),).length;const progressValue =row.tasks.length === 0 ? 0 : (completed / row.tasks.length) * 100;return (<TableRow key={row.participantId}><TableHead scope="row">{row.displayName}</TableHead><TableCell><ul className={styles.list}>{row.tasks.length === 0 ? (<li className={styles.muted}>No general tasks assigned.</li>) : (row.tasks.map((task) => (<li key={task.taskId}><strong>{task.title}</strong> · {dateLabel(task.dueAt)} ·{" "}<SpeakerTaskStatusBadge status={task.status} /></li>)))}</ul></TableCell><TableCell><div className={styles.progressCell}><Progress value={progressValue}aria-label={`${completed} of ${row.tasks.length} tasks complete`}/><span>{completed} / {row.tasks.length} complete</span></div></TableCell></TableRow>);})}</TableBody></Table>) : null}</CardContent></Card><div ref={reminderSectionRef}><Card className={styles.panel}><CardHeader className={styles.panelHeader}><div><CardTitle id="upcoming-reminders-heading">Upcoming reminders</CardTitle><CardDescription>Only reminders that are currently eligible for delivery are shown here.</CardDescription></div><Badge variant="outline">{reminderEligibility === null ? "Loading" : `${eligibleReminderItems.length} due`}</Badge></CardHeader><CardContent className={styles.actionsStack}>{reminderEligibility === null ? (<FormMessage message="Checking upcoming reminders…" />) : eligibleReminderItems.length === 0 ? (<Empty><EmptyTitle>No eligible reminders</EmptyTitle><EmptyDescription>Eligible reminders will appear here when their delivery window opens.</EmptyDescription></Empty>) : (<ul className={styles.list} aria-label="Upcoming reminders">{eligibleReminderItems.map((item) => (<li className={styles.reminderItem} key={item.taskId}><strong>{item.title}</strong><span>{dateLabel(item.dueAt)}</span><Badge variant="secondary">Ready to send</Badge></li>))}</ul>)}{ineligibleReminderItems.length > 0 ? (<Accordion type="single" collapsible defaultValue=""><AccordionItem value="diagnostics"><AccordionTrigger>Reminder diagnostics</AccordionTrigger><AccordionContent><p className={styles.muted}>Internal eligibility reasons are available for operators who need to investigate a reminder schedule.</p><ul className={styles.list}>{ineligibleReminderItems.map((item) => (<li key={item.taskId}><strong>{item.title}</strong> · {item.reason}</li>))}</ul></AccordionContent></AccordionItem></Accordion>) : null}</CardContent></Card></div></>) : null}</TabsContent><TabsContent value="email"id="email-view"aria-labelledby="email-tab"className={styles.view}><div ref={emailSectionRef}>{rosterEmpty ? (<Empty className={styles.empty}><EmptyHeader><EmptyMedia variant="icon"><Mail /></EmptyMedia><EmptyTitle>Add speakers before emailing</EmptyTitle><EmptyDescription>Add or import speakers before sending speaker-only outreach for this event.</EmptyDescription></EmptyHeader><div className={styles.actions}><Button variant="default" type="button" onClick={() => {setActiveView("roster");setShowAdd(true);}}><UserPlus data-icon="inline-start" />Add speaker</Button><Button variant="outline" type="button" onClick={() => {setActiveView("roster");setShowCsv(true);}}><FileText data-icon="inline-start" />Import CSV</Button></div></Empty>) : scopedRoster !== null && selectedSpeakerIds.length === 0 ? (<Empty className={styles.empty}><EmptyHeader><EmptyMedia variant="icon"><Users /></EmptyMedia><EmptyTitle>Choose recipients</EmptyTitle><EmptyDescription>Select speakers in Roster before composing speaker-only outreach. Broader announcements belong in Communications.</EmptyDescription></EmptyHeader><Button variant="default" type="button" onClick={() => setActiveView("roster")}><Users data-icon="inline-start" />Choose recipients</Button></Empty>) : (<><Card className={styles.panel} aria-busy={emailAnyBusy}><CardHeader className={styles.panelHeader}><div><CardTitle id="bulk-email-heading">Speaker email</CardTitle><CardDescription>Use this event-scoped Email workspace for speaker-only outreach; broader announcements belong in Communications. Compose a message for {selectedSpeakerIds.length} selected speaker{selectedSpeakerIds.length === 1 ? "" : "s"}, save a draft, preview selected recipients, then confirm the send. Start with a blank message or apply an editable starter.</CardDescription></div><Badge variant="outline">Preview required before send</Badge></CardHeader><CardContent className={styles.actionsStack}><div className={styles.emailFlowGrid}><div className={styles.emailEditor}><div className={styles.emailTemplateRow}><Field><FieldLabel htmlFor="email-template">Template version</FieldLabel><Select value={emailTemplateId? `${emailTemplateId}:${emailTemplateVersion ?? ""}`: "new"}onValueChange={(value) => {invalidateEmailPreview();if (value === "new") {setEmailTemplateId("");setEmailCreateTemplateId(null);setEmailTemplateVersion(undefined);return;}const separator = value.lastIndexOf(":");const nextId = separator < 0 ? value : value.slice(0, separator);const rawVersion = separator < 0 ? "" : value.slice(separator + 1);const nextVersion = Number(rawVersion);const template = emailTemplates.find((candidate) =>candidate.id === nextId && candidate.version === nextVersion,);setEmailTemplateId(nextId);setEmailCreateTemplateId(null);setEmailTemplateVersion(Number.isFinite(nextVersion) ? nextVersion : undefined,);if (template !== undefined) {setEmailTemplateName(template.name);setEmailSubject(template.subject);setEmailHtml(template.html);setEmailText(template.text);}}}disabled={emailSaveBusy}><SelectTrigger id="email-template"><SelectValue placeholder="New template version" /></SelectTrigger><SelectContent><SelectGroup><SelectItem value="new">New template version</SelectItem>{emailTemplates.map((template) => (<SelectItem key={`${template.id}:${template.version}`}value={`${template.id}:${template.version}`}>{template.name} · v{template.version} · {template.status}</SelectItem>))}</SelectGroup></SelectContent></Select></Field><div className={styles.emailTemplateMeta} aria-live="polite"><strong>{selectedEmailTemplate?.name ?? emailTemplateName}</strong><span className={styles.muted}>{emailTemplateId? `Exact template ${emailTemplateId} · version ${emailTemplateVersion ?? "unsaved"}`: "New draft · save to create an exact server version"}</span>{selectedEmailTemplate ? (<span className={styles.muted}>{statusLabel(selectedEmailTemplate.status)} · Sender{" "}{selectedEmailTemplate.sender}</span>) : null}</div></div><Field><div className={styles.actions}><FieldLabel htmlFor="email-template-name">Template name</FieldLabel><Button type="button"variant="outline"size="sm"disabled={emailSaveBusy}onClick={() => {setEmailTemplateId("");setEmailCreateTemplateId(null);setEmailTemplateVersion(undefined);setEmailTemplateName(SPEAKER_WELCOME_EMAIL_STARTER.name);setEmailSubject(SPEAKER_WELCOME_EMAIL_STARTER.subject);setEmailHtml(SPEAKER_WELCOME_EMAIL_STARTER.html);setEmailText(SPEAKER_WELCOME_EMAIL_STARTER.text);setEmailEditorMode("visual");invalidateEmailPreview();}}>Use welcome starter</Button></div><Input id="email-template-name"value={emailTemplateName}onChange={(event) => {setEmailTemplateName(event.target.value);invalidateEmailPreview();}}maxLength={200}disabled={emailSaveBusy}/></Field><Field><FieldLabel htmlFor="email-subject">Subject</FieldLabel><Input id="email-subject"value={emailSubject}onChange={(event) => {setEmailSubject(event.target.value);invalidateEmailPreview();}}placeholder="Add a clear subject for {{first_name}}"maxLength={500}disabled={emailSaveBusy}/></Field><Tabs value={emailEditorMode}onValueChange={(value) =>setEmailEditorMode(value as "visual" | "html" | "text")}className={styles.emailEditorTabs}><TabsList variant="line"className={styles.emailEditorTabsList}aria-label="Email editor mode"><TabsTrigger value="visual">Visual preview</TabsTrigger><TabsTrigger value="html">HTML source</TabsTrigger><TabsTrigger value="text">Plain text</TabsTrigger></TabsList><TabsContent value="visual" className={styles.actionsStack}><p className={styles.muted}>Visual mode uses the safe server preview. Raw HTML is never executed in this workspace.</p>{emailPreviewCurrent && emailPreview ? (<div className={styles.emailPreviewOutput}><p className={styles.muted}>Server-rendered text</p><pre>{emailPreview.text}</pre><p className={styles.muted}>Escaped HTML output</p><pre>{emailPreview.html}</pre></div>) : (<p className={styles.muted} role="status">Preview selected recipients to see the server-rendered result.</p>)}</TabsContent><TabsContent value="html"><Field><FieldLabel htmlFor="email-html">HTML source</FieldLabel><Textarea id="email-html"value={emailHtml}onChange={(event) => {setEmailHtml(event.target.value);invalidateEmailPreview();}}placeholder="<p>Hello {{first_name}},</p><p>Add your message here.</p>"maxLength={100_000}disabled={emailSaveBusy}/></Field></TabsContent><TabsContent value="text"><Field><FieldLabel htmlFor="email-text">Plain text body</FieldLabel><Textarea id="email-text"value={emailText}onChange={(event) => {setEmailText(event.target.value);invalidateEmailPreview();}}placeholder={"Hello {{first_name}},\n\nAdd your message here."}maxLength={100_000}disabled={emailSaveBusy}/></Field></TabsContent></Tabs><p className={styles.muted}>Merge variables are resolved by the server:{" "}<code className={styles.code}>{"{{first_name}}"}</code>,{" "}<code className={styles.code}>{"{{display_name}}"}</code>,{" "}<code className={styles.code}>{"{{email}}"}</code>.</p></div><Card size="sm"className={styles.emailPreviewPanel}aria-label="Selected speaker email preview"><CardHeader><CardTitle>Preview selected recipients</CardTitle><CardDescription>Exact server result; this panel never executes template HTML.</CardDescription></CardHeader><CardContent className={styles.actionsStack}>{emailPreviewCurrent && emailPreview ? (<><p className={styles.muted}>{emailPreview.recipientIds.length} recipient{emailPreview.recipientIds.length === 1 ? "" : "s"} · exact template{" "}{emailPreview.templateId} · version {emailPreview.templateVersion}</p><p><strong>Subject:</strong> {emailPreview.subject}</p><ul className={styles.list}aria-label="Speaker email preview recipient names">{emailPreview.recipients.map((recipient) => (<li key={recipient.participantId}><strong>{recipient.displayName}</strong> · {recipient.email}</li>))}</ul><div className={styles.emailPreviewOutput}><p className={styles.muted}>Server-rendered text</p><pre>{emailPreview.text}</pre><p className={styles.muted}>Escaped HTML output</p><pre>{emailPreview.html}</pre></div></>) : (<p className={styles.muted} role="status">No current preview. Select recipients and preview before confirming a send.</p>)}</CardContent></Card></div></CardContent><CardFooter className={styles.actions}><Button variant="outline"type="button"onClick={() => void saveEmailTemplate()}disabled={emailSaveBusy || api === null}><CheckCircle2 data-icon="inline-start" />{emailSaveBusy ? "Saving…" : "Save draft"}</Button><Button variant="secondary"type="button"onClick={() => void previewBulkEmail()}disabled={emailPreviewBusy || api === null || selectedSpeakerIds.length === 0}><Eye data-icon="inline-start" />{emailPreviewBusy ? "Preparing…" : "Preview selected recipients"}</Button><Button variant="default"type="button"onClick={() => setEmailConfirmOpen(true)}disabled={emailSendBusy || api === null || !emailPreviewCurrent}><Send data-icon="inline-start" />{emailSendBusy ? "Queueing…" : "Confirm send"}</Button><Button variant="ghost"size="sm"type="button"onClick={() => void refreshEmailHistory()}disabled={emailHistoryBusy || api === null}aria-label="Refresh speaker email history"><RefreshCw data-icon="inline-start" />{emailHistoryBusy ? "Refreshing history…" : "Refresh history"}</Button></CardFooter><CardContent className={styles.actionsStack}>{emailNotice ? (<FormMessage message={emailNotice}error={emailNotice.includes("unavailable") || emailNotice.includes("could")}/>) : null}<Card size="sm" className={styles.emailHistory}><CardHeader><CardTitle>Email send history</CardTitle><CardDescription>Completed send records stay here when you start a new draft or preview.</CardDescription></CardHeader><CardContent>{emailSends.length === 0 ? (<p className={styles.muted} role="status">No email sends recorded for this event.</p>) : (<Table><TableCaption className={adminStyles.srOnly}>Speaker email send history</TableCaption><TableHeader><TableRow><TableHead>Status</TableHead><TableHead>Template</TableHead><TableHead>Recipients</TableHead><TableHead>Updated</TableHead></TableRow></TableHeader><TableBody>{emailSends.map((send) => (<TableRow key={send.id}><TableCell><Badge variant="outline">{send.status}</Badge></TableCell><TableCell>{send.templateId} · v{send.templateVersion}</TableCell><TableCell>{send.recipientIds.length}</TableCell><TableCell>{dateLabel(send.updatedAt)}</TableCell></TableRow>))}</TableBody></Table>)}</CardContent></Card></CardContent></Card><AlertDialog open={emailConfirmOpen} onOpenChange={setEmailConfirmOpen}><AlertDialogContent className={styles.dialogContent}><AlertDialogHeader><AlertDialogTitle>Confirm speaker email send</AlertDialogTitle><AlertDialogDescription>Queue the current server preview for {emailPreview?.recipientIds.length ?? 0}{" "}selected recipient{(emailPreview?.recipientIds.length ?? 0) === 1 ? "" : "s"} using exact template version {emailPreview?.templateVersion ?? "unavailable"}. This action uses the current idempotency key and cannot be edited after queueing.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel disabled={emailSendBusy}>Cancel</AlertDialogCancel><AlertDialogAction disabled={emailSendBusy || !emailPreviewCurrent}onClick={() => void sendBulkEmail()}>{emailSendBusy ? "Queueing…" : "Confirm send"}</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></>)}</div></TabsContent></Tabs></div>);}
